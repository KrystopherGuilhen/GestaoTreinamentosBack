# Banco de dados em container (ex: usando Docker Compose com serviço chamado "db")
spring.datasource.url=jdbc:mysql://db:3306/gestao-treinamentos
spring.datasource.username=root
spring.datasource.password=senha_segura_prod

# Hibernate: não altera esquema automaticamente (segurança)
spring.jpa.hibernate.ddl-auto=validate

# Configurações de segurança do Spring Boot (exemplo)
management.endpoints.web.exposure.include=health,info